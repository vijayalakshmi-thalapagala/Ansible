- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" # Item is reservce keyword
    loop:
    - Vijju
    - Lucky
    - reddy
